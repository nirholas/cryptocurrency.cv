"use client";

import { type ReactNode } from "react";

/** Minimal stub – bookmarks feature will be rebuilt later */
export function BookmarksProvider({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
