"use client";

import { type ReactNode } from "react";

/** Minimal PWA provider stub – will be rebuilt */
export function PWAProvider({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
